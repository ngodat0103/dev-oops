# This inventory describe a HA typology with stacked etcd (== same nodes as control plane)
# and 3 worker nodes
# See https://docs.ansible.com/ansible/latest/inventory_guide/intro_inventory.html
# for tips on building your # inventory

# Configure 'ip' variable to bind kubernetes services on a different ip than the default iface
# We should set etcd_member_name for etcd cluster. The node that are not etcd members do not need to set the value,
# or can set the empty string value.

[all:vars]
ansible_user=root
ansible_ssh_private_key_file = "~/OneDrive/credentials/ssh/k8s/prod/id_rsa" 

[kube_control_plane]
master-node ansible_host=192.168.1.200  ip=192.168.1.180  etcd_member_name=etcd0

[etcd:children]
kube_control_plane

[kube_node]
traefik ansible_host=192.168.1.190  ip=192.168.1.190 ansible_ssh_private_key_file = "~/OneDrive/credentials/ssh/traefik/id_rsa" 
