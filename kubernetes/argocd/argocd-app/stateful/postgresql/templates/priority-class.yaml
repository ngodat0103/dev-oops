apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: database-critical
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
value: 1000000
globalDefault: false
preemptionPolicy: PreemptLowerPriority
description: "Stateful databases — schedule first, may preempt lower tiers"