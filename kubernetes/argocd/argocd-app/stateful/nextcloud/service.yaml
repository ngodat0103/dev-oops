apiVersion: v1
kind: Service
metadata:
  name: nextcloud
  namespace: nextcloud
spec:
  selector:
    app: nextcloud
  ports:
  # Traefik terminates TLS; backend uses HTTP inside the cluster (HTTPS to ClusterIP fails cert SAN checks).
  - name: http
    port: 80
    targetPort: 80
