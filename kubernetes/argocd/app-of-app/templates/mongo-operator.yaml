{{- if and (.Values.mongoOperator) (.Values.mongoOperator.enabled) -}}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mongo-operator
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/ngodat0103/dev-oops.git
    path: kubernetes/charts/mongo-operator
    targetRevision: master
  destination:
    name: in-cluster
    namespace: mongo
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 1
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 1m
{{- end -}}